name: appstore_screenshots
description: 'A Flutter package for automating app store screenshots with
  customizable backgrounds and templates.'
version: 0.1.0
homepage: https://3labs.app

environment:
  sdk: ^3.8.1
  flutter: '>=1.17.0'

dependencies:
  archive: ^3.4.10
  file_picker: ^10.2.0
  flutter:
    sdk: flutter
  path: ^1.8.3
  path_provider: ^2.1.5
  share_plus: ^11.0.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
