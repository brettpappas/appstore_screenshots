name: appstore_screenshots_example
description: Example app demonstrating the appstore_screenshots package.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.8.1
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter
  appstore_screenshots:
    path: ../
  file_picker: ^8.1.2
  share_plus: ^10.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
